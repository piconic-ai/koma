{
  "name": "koma",
  "description": "Code, frame by frame",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run build:export && concurrently -k -n build,uno,server -c blue,magenta,green \"bunx @barefootjs/cli build --watch\" \"unocss --watch\" \"bunx wrangler dev --live-reload\"",
    "build": "bunx @barefootjs/cli build && unocss && bun run build:export",
    "deploy": "bun run build && bunx wrangler deploy",
    "watch": "concurrently -k -n build,uno -c blue,magenta \"bunx @barefootjs/cli build --watch\" \"unocss --watch\"",
    "build:export": "bun scripts/build-export-bundle.mjs",
    "test": "bun test"
  },
  "dependencies": {
    "@barefootjs/cli": "0.11.0",
    "@barefootjs/client": "0.11.0",
    "@barefootjs/hono": "0.11.0",
    "@barefootjs/jsx": "0.11.0",
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@barefootjs/test": "0.11.0",
    "@cloudflare/workers-types": "^4.20250101.0",
    "@types/bun": "latest",
    "@unocss/cli": "^66.0.0",
    "@unocss/preset-wind4": "^66.0.0",
    "concurrently": "^9.0.0",
    "shiki": "^4.1.0",
    "typescript": "^5.6.0",
    "unocss": "^66.0.0"
  }
}
